// class Point {
//   //attributes (public, private, protected)
//   //methods (functions/ name & arrow)(we don't write let const with methods)
// }

class Car {
  model: string;
  year: number;
  price: string;
  drive() {
    console.log("The Car has Started driving");
  }
  stop() {
    console.log("The car has stopped");
  }
}
