# Student Management System CLI

A CLI-based Student Management System to manage student enrollments, fees, and statuses using TypeScript and inquirer.

## Features

- Add new students to the system.
- Enroll students in courses.
- View student balances.
- Pay student fees.
- Display student statuses.

## Installation

First, ensure you have Node.js and npm installed. Then, you can install the package using npm:

```bash
npm ts_cli_student_management_system
```

## Usage

To use the Word Counter CLI, run the following command in your terminal:

```bash
npx ts_cli_student_management_system
```
